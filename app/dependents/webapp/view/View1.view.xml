<mvc:View controllerName="ns.dependents.controller.View1"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">
    <Page id="page" title="{i18n>title}">
        <content >
            <VBox id="vbox" binding="{ path: '/Books(1)'}">
                <Button 
                    id="btn"    
                    press="onButtonPressed"  
                    text="{name}">
                </Button>
            </VBox>
        </content>
    </Page>
</mvc:View>
